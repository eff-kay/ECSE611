[(619, 619, "      // Some unit tests don't need a cluster, so no zookeeper at all"), (620, 620, '      if (!conf.getBoolean("hbase.testing.nocluster", false)) {'), (621, 621, '        // Open connection to zookeeper and set primary watcher'), (622, None, '        zooKeeper = new ZKWatcher(conf, getProcessName() + ":" +'), (623, None, '          rpcServices.isa.getPort(), this, canCreateBaseZNode());'), (None, 622, '        zooKeeper = new ZKWatcher(conf, getProcessName() + ":" + rpcServices.isa.getPort(), this,'), (None, 623, '            canCreateBaseZNode(), false, true);'), (624, 624, '        // If no master in cluster, skip trying to track one or look for a cluster status.'), (625, 625, '        if (!this.masterless) {'), (626, 626, '          if (conf.getBoolean(HBASE_SPLIT_WAL_COORDINATED_BY_ZK,')]
